{
  "name": "tiny-signal",
  "version": "3.0.0-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prepare-hosting.mjs",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "generate:sfx": "node scripts/generate-sfx.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
